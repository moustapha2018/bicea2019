{% extends 'template.html.twig' %}
{% block title %}Modification des informations colaborateur{% endblock %}
{% block body %}


    <div class="container" >
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }} mt-10">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}

        <h4><span class="glyphicon glyphicon-edit"></span> Modification des informations</h4>
        {# <a href="{{ asset('uploads/contrats/' ~ user.contrat) }}">View brochure (PDF)</a> #}
        {{ form(form) }}



    </div>



{% endblock %}